{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autoplatform.dev/contracts/v1/payment-intent.schema.json",
  "title": "PaymentIntent",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "company_id",
    "correlation_id",
    "event_id",
    "idempotency_key",
    "payment_intent_id",
    "amount_cents",
    "method",
    "status",
    "occurred_at"
  ],
  "properties": {
    "schema_version": { "const": "1" },
    "company_id": { "type": "string", "format": "uuid" },
    "correlation_id": {
      "type": "string",
      "minLength": 16,
      "maxLength": 64,
      "pattern": "opaque-id"
    },
    "event_id": {
      "type": "string",
      "minLength": 16,
      "maxLength": 64,
      "pattern": "opaque-id"
    },
    "idempotency_key": {
      "type": "string",
      "minLength": 16,
      "maxLength": 64,
      "pattern": "opaque-id"
    },
    "payment_intent_id": { "type": "string", "format": "uuid" },
    "amount_cents": { "type": "integer", "minimum": 0 },
    "method": { "type": "string", "enum": ["pix", "card"] },
    "status": {
      "type": "string",
      "enum": ["created", "pending", "succeeded", "failed", "indeterminate"]
    },
    "occurred_at": { "type": "string", "format": "date-time" }
  }
}
